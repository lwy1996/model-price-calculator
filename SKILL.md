---
name: model-price-calculator
description: 计算大模型 token 价格、倍率价格、充值比折算价格和中转站性价比对比，并支持从截图、价格卡片图片或杂乱文本中提取输入/输出/缓存价格。用户提到模型价格、输入/输出/缓存单价、倍率、充值比、分组、每百万价格、站点定价、换算文案、价格对比、成本分析、回本倍率、利润率，或要求 Codex 根据缺失参数主动追问后再计算时使用。
---

# Model Price Calculator

按统一规则收集价格参数，缺项先追问，再调用脚本计算并输出可复制文案、人民币折算价和对比指标。若用户提供的是截图或价格卡片图片，先从图片中提取字段，再补问缺项。

除了单次计算外，这个 skill 还负责维护“中转站价格库”：
- 记录每个中转站的别名、官网地址、API 请求地址
- 记录同一个中转站下的不同模型、不同分组价格
- 如果站点未收录，先收录再写价格
- 如果站点已收录，先检索再更新对应模型/分组
- 每次收录或更新后，按同模型或同分组重新排序

这个 skill 默认采用“引导式补录”：
- 不要求用户一次性整理完整字段
- 不要求每次都写字段标题
- 优先根据用户当前给出的最少信息继续推进
- 每次只追问当前动作的最小必要字段
- 如果用户只是想先整理资料，优先给一份“可复制填写模板”

默认存储后端是本地 JSON 文件：
`assets/site-price-registry.json`

多轮会话草稿保存在：
`assets/site-price-drafts.json`

这是为了在 skill 阶段做到：
- 零依赖
- 可直接随技能分发
- 可本地备份和人工查看
- 后续容易迁移到 SQLite / MySQL

价格库还支持“测试数据标识”：
- 可在站点级写入 `is_test_data=true`
- 测试站点会在列表中单独标识
- 后续可统一执行清理，不影响正式录入站点

## 工作流

1. 先识别用户是否已经提供以下字段：
`模型名称`、`分组`、`输入价格`、`输出价格`、`缓存价格`、`倍率`、`充值比`

2. 其中字段规则如下：
- `模型名称` 默认 `gpt5.4`
- `分组` 可选，但如果用户想生成渠道文案，优先追问
- `输入价格` 必填
- `输出价格` 必填
- `缓存价格` 默认优先使用 `缓存读取价格`
- 如果同时给出 `缓存读取价格` 和 `缓存创建价格`，都要算，并在结果中分开显示
- `倍率` 默认 `1`
- `充值比` 默认 `1:1`
- 如果用户没有特别说明汇率/充值比，则默认按 `美元=人民币=1:1` 处理，也就是 `$2.5 => ¥2.5`
- 只有用户明确给出 `1:1`、`2:3`、`29.99:1000` 这类比例时，才覆盖默认值
- 如果已经识别到是同一个已收录站点，且同模型已有历史记录：
  - 用户没特别说明 `分组` 时，优先沿用该模型最近一次已录入的分组
  - 用户没特别说明 `倍率` 时，优先沿用该模型最近一次已录入的倍率
  - 只有在没有可继承历史记录时，才回退到默认 `分组=default`、`倍率=1`

3. 若缺少必要字段，只追问缺失项，不重复问已给出的内容。推荐用紧凑格式追问：
`还差 1 个参数：缓存价格。如果没有单独缓存价，我可以按“缓存读取价格”口径来算；倍率默认按 1，充值比默认按 1:1 处理。`

4. 参数齐全后，优先运行：
`scripts/calc_model_price.py`

## 引导式输入规则

1. 如果用户说“记录一个中转站价格”但没给完整信息，不要一次性抛出长表单。

2. 优先按这个顺序补问：
- 先确认中转站身份
  - 至少拿到：`别名`、`官网`、`API 地址` 三者之一
- 再确认价格归属
  - 至少拿到：`模型名称`
  - 如果涉及多个档位，再问：`分组`
- 最后确认价格
  - 至少拿到：`输入价格`、`输出价格`
  - 缓存价格、倍率、充值比按已有默认值处理，缺失时不强制追问

3. 如果用户只说：
`帮我记一下 xx 站`
则先问：
`先给我一个能识别这个站的信息即可：站点别名、官网地址、或 API 地址，三选一就行。`

4. 如果用户只说：
`更新 alpha 的官网`
则不要追问价格，直接进入站点字段更新流程。

5. 如果用户只说：
`把 alpha 的 gpt5.4 pro 输入价改成 2.4`
则不要追问其他价格项，直接局部更新该字段。

6. 每次追问只问 1 到 2 个最关键字段，避免像表单一样压用户。

## 模板输出规则

1. 如果用户说法接近以下意图，优先先给“可复制填写模板”：
- `给我个模板`
- `我先整理一下`
- `你给我一个填写格式`
- `我慢慢补`
- `先给我个录入模板`

2. 模板原则：
- 所有字段默认都是非必填
- 不要写“必填/非必填”标签，避免增加压力
- 模板尽量短，方便复制后直接改
- 如果上下文已经知道某些值，可以预填；不知道就留空

3. 默认使用这份通用模板：

```text
站点名称：
官网：
API：
倍率：
分组：
模型名称：
价格信息：
备注：
```

4. 如果用户已经明确了某些字段，输出“带预填值的模板”。例如用户已说分组是 `vip`，则可输出：

```text
站点名称：
官网：
API：
倍率：
分组：vip
模型名称：
价格信息：
备注：
```

5. `价格信息` 一栏下允许用户自由粘贴原始文本，不强制拆成字段。常见写法包括：
- `输入 $2.5000/M`
- `输出 $20.0000/M`
- `缓存读取 $0.2500/M`
- 或整段截图识别文本

6. 如果用户填完模板后回传，不要求字段顺序完全一致；有啥取啥，剩余字段再按引导式规则补问。

## 会话态草稿规则

1. 如果用户信息还不完整，先写草稿，不急着正式入库：
`scripts/draft_site_price.py merge --json-file <payload>`

2. 每次 merge 后都要关注草稿状态：
- 当前已收集到哪些站点字段
- 当前已收集到哪些价格字段
- 还缺哪些必要字段
- 下一步最该问哪 1 到 2 个问题

3. 草稿满足最小入库条件时，再执行正式提交：
`scripts/draft_site_price.py commit --json-file <payload>`

最小入库条件：
- 至少能识别站点：别名 / 官网 / API 地址 三者之一
- 模型名称
- 输入价格
- 输出价格

4. 如果用户问“我现在记到哪了”或“看下当前草稿”，执行：
`scripts/draft_site_price.py show --json-file <payload>`

5. 如果用户说“算了”“清空这个草稿”，执行：
`scripts/draft_site_price.py clear --json-file <payload>`

## 中转站价格库工作流

1. 如果用户提到“某个中转站”“站点别名”“官网地址”“API 请求地址”“收录”“更新”“重排”，优先走价格库流程。

2. 识别站点字段：
- `站点别名`
- `站点名称`
- `官网地址`
- `API 请求地址`

3. 如果用户同时给了价格信息：
- 先检索是否已收录：
  `scripts/site_price_registry.py search --json-file <payload>`
- 未收录：执行 `upsert`
- 已收录：继续用 `upsert` 覆盖同站点 + 同模型 + 同分组记录，或新增新分组记录

4. 每次 `upsert` 后，自动再执行一次 `rank`：
- 如果用户指定模型，则按该模型排序
- 如果用户指定模型和分组，则按该模型 + 分组排序
- 默认排序字段优先使用 `summary_rmb_per_m`

5. 检索匹配规则：
- 优先用别名命中
- 其次用官网地址命中
- 再用 API 请求地址命中
- 命中后合并新增别名，避免重复收录同一中转站

6. 如果用户要“一键入库”，优先走统一入口：
`scripts/ingest_site_price.py`

它负责串起：
- 文本/图片提取
- 站点收录或更新
- 价格计算
- 自动重排

7. 如果用户一次给出的是“一个站点 + 多个分组倍率 + 一个或多个模型价格块”，优先走批量入口：
`scripts/batch_ingest_site_price.py`

适用场景示例：
- 一个站点有 `free / plus / pro` 多个分组倍率
- 一次贴了 `gpt-5.4`、`gpt-5.5` 多个模型价格
- 希望自动拆成多条记录，而不是逐条手工录入
- 文本中明确说明“以下都是倍率后的价格”，希望自动反推回原始价再入库
- 文本中写了 `缓存读取价格` 或 `充值比`，希望自动套用到拆分后的记录

## 图片识别规则

1. 如果用户附带截图、价格卡片或界面照片，先直接阅读图片内容，不要立刻要求用户转文字。
2. 从图片里优先提取：
- 模型名称
- 输入价格
- 输出价格
- 缓存读取价格
- 缓存创建价格
- 货币符号

3. 图片中常见模式包括：
- 标题行是模型名，例如 `gpt-5.4`
- 多行价格文本，例如 `输入价格 ¥7.0000 / 1M Tokens`
- 简洁行文本，例如 `输入 $2.5000/M`

4. 如果图片里只出现人民币符号 `¥`，且用户没有提供充值比，直接按 `1:1` 继续计算，不额外追问充值比。
5. 如果图片里既没有模型名也没有分组，但价格齐全，可以先计算，再在结果中把模型名默认写为 `gpt5.4`，并说明是默认值。

## 支持输入格式

- 金额可写成：`2.5`、`$2.5000`、`￥0.83`
- 金额行可写成：`$2.5000 / 1M Tokens`、`$2.5000/M`、`¥7.0000 / 1M Tokens`
- 价格标签可写成：`输入价格`、`输入单价`、`Prompt`、`Input`、`输入`
- 输出标签可写成：`输出价格`、`补全价格`、`Completion`、`Output`、`输出`
- 缓存标签可写成：`缓存价格`、`缓存读取价格`、`Cache Read`、`缓存读取`
- 缓存创建标签可写成：`缓存创建价格`、`Cache Write`、`缓存创建`
- 充值比可写成：
  - `1:1`
  - `2:3`
  - `29.99:1000`
  - `人民币/额度 = 29.99:1000`

也支持这种分行格式：

```text
输入价格 $1.1170
/ 1M Tokens
补全价格 $6.7020
/ 1M Tokens
缓存读取价格 $0.1120
/ 1M Tokens
```

也支持这种紧凑格式：

```text
输入 $2.5000/M
输出 $20.0000/M
```

## 计算规则

设：
- 输入单价 = `X`
- 输出单价 = `A`
- 缓存单价 = `B`
- 倍率 = `Y`
- 充值比 = `RMB / Credit`，默认 `1:1`

则：
- 输入倍率价 = `X * Y`
- 输出倍率价 = `A * Y`
- 缓存倍率价 = `B * Y`
- 输入人民币每百万 = `X * Y * 充值比`
- 输出人民币每百万 = `A * Y * 充值比`
- 缓存人民币每百万 = `B * Y * 充值比`
- 综合基础价 = `X + A + B`
- 综合倍率价 = `Y * (X + A + B)`
- 综合人民币价 = `Y * (X + A + B) * 充值比`

如果同时存在缓存读取和缓存创建：
- 综合基础价 = `X + A + cache_read + cache_write`
- 所有综合项按同样逻辑累加

## 输出要求

默认输出 4 块内容：

1. 可复制主文案
格式：
`模型名称-分组-输入$X-输出$A-缓存$B`

如果同时提供缓存读取和缓存创建，改为：
`模型名称-分组-输入$X-输出$A-缓存读$B1-缓存写$B2`

2. 倍率折算结果
输出每个维度乘倍率后的价格。

3. 人民币折算结果
输出每个维度对应的 `¥/1M Tokens`。

4. 对比指标
至少给出：
- 输入/输出/缓存的人民币单价
- 综合人民币单价
- 输出/输入比
- 缓存读取折扣率（相对输入）
- 缓存创建折扣率或溢价率（相对输入）

5. 如果输出内容本身已经是 Markdown 列表、Markdown 表格或完整 Markdown 文本：
- 默认直接以可渲染 Markdown 形式输出
- 不要再额外包一层代码块
- 只有用户明确要求“原始 Markdown 源文本”时，才放进代码块

## 对比分析规则

当用户一次给出多个方案、多个分组或多个站点时：

1. 对每个方案分别计算结果。
2. 追加横向对比，至少包含：
- 输入人民币单价从低到高排序
- 输出人民币单价从低到高排序
- 综合人民币单价从低到高排序
- 缓存最便宜方案
- 输出/输入比最低方案

3. 如果用户给出了“售卖价”或“站点售价”，再额外计算：
- 单项毛利 = `售价 - 成本`
- 单项毛利率 = `毛利 / 售价`
- 回本倍率 = `售价 / 基础官方价`

## 执行方式

优先用脚本，避免手算误差。Windows PowerShell 下建议优先使用 `--json-file`，不要直接拼长 JSON 字符串：

```powershell
$payloadPath = "C:\path\to\payload.json"
python "<skill_dir>/scripts/calc_model_price.py" --json-file $payloadPath
```

如果当前环境不方便写文件，也可用：

```powershell
python "<skill_dir>/scripts/calc_model_price.py" --json "<json-string>"
```

脚本返回 JSON，随后整理成人类可读结果。

## 参考

需要示例输入或字段映射时，读取：
`references/examples.md`

如果用户提供的是图片或杂乱文本，先运行：
`scripts/extract_model_price.py`

再把提取结果交给：
`scripts/calc_model_price.py`

如果用户要维护价格库，再运行：
`scripts/site_price_registry.py`

如果用户要一键入库，运行：
`scripts/ingest_site_price.py`

如果用户是批量结构录入，运行：
`scripts/batch_ingest_site_price.py`

如果用户要按模型输出排行榜文案，运行：
`scripts/site_price_registry.py leaderboard --json-file <payload>`

如果用户要以更好看的 Markdown 列表查看全部站点，运行：
`scripts/site_price_registry.py stations-md --json-file <payload>`

执行 `stations-md`、`leaderboard` 或其他已返回 Markdown 文本的展示型命令时：
- 优先直接渲染脚本返回的 Markdown
- 不要自行改写结构或重新排版
- 不要默认包裹三引号代码块，除非用户明确要求查看原始 Markdown

如果用户要统一清理测试站点，运行：
`scripts/site_price_registry.py cleanup-test --json-file <payload>`

如果用户要只更新站点信息，运行：
`scripts/site_price_registry.py update-station --json-file <payload>`

如果用户要只更新某个模型/分组的一项价格，运行：
`scripts/site_price_registry.py patch-record --json-file <payload>`

如果用户是多轮补录，运行：
`scripts/draft_site_price.py`
