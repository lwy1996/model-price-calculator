# model-price-calculator Project Rules

## Identity
- 本目录是技能 `model-price-calculator` 的源开发目录，不是最终使用中的技能目录。
- 技能维护、规则维护、数据维护以本项目为准；本目录改动由外部同步脚本/流程同步到实际使用目录。
- 在本项目语境下，“使用这个技能”来维护规则、录入数据或调整脚本，默认都落到本开发目录。

## Rules
- 进入项目后，先阅读 `docs/project-context.md`，再进行代码、提示词、数据或文档修改。
- 涉及技能规则、提示词、示例、价格库、脚本入口调整时，先确认属于“源目录维护”，不要直接修改同步后的产物目录。
- 未经用户明确说明，不新增并行规则来源，避免 `SKILL.md`、数据文件、脚本逻辑彼此不一致。
- 优先复用既有目录约定：`SKILL.md`、`assets/`、`scripts/`、`references/`、`agents/`；新增说明文档优先放 `docs/`，并区分项目事实与操作流程。

## Validation
- 优先做静态阅读、局部命令帮助检查、语法检查等无副作用验证。
- 未经用户明确同意，不执行可能改写价格库、覆盖同步产物或触发外部同步的命令。
