# 项目级 Agent 说明

## 项目定位
- 本目录是技能 `model-price-calculator` 的源开发目录。
- 用户在日常使用该技能时，实际调用的是技能使用目录中的内容，不是本开发目录中的文件。
- 技能的维护、规则维护、数据维护以本项目为准，不以最终使用中的技能目录为准。
- 本目录内的修改会通过外部同步脚本/流程同步到实际使用的技能目录，因此默认应只修改本项目。
- 在本项目语境下，凡是“使用这个技能”来维护规则、录入数据、调整脚本，默认都应落到本开发目录，再由用户手动同步到技能使用目录。

## 工作约束
- 进入项目后，先阅读 `docs/project-context.md`，再进行代码、提示词、数据或文档修改。
- 若涉及技能规则、提示词、示例、价格库、脚本入口调整，先确认改动属于“源目录维护”，不要直接假设应修改同步后的产物目录。
- 未经用户明确说明，不要在本项目中新增另一套并行规则来源，避免出现 `SKILL.md`、数据文件、脚本逻辑彼此不一致。

## 修改偏好
- 优先复用当前项目已有目录约定：
  - `SKILL.md`：技能主说明
  - `assets/`：价格库与草稿数据
  - `scripts/`：计算、提取、入库相关脚本
  - `references/`：示例与参考材料
  - `agents/`：代理配置
- 若新增说明文档，优先放在 `docs/`，并保持“项目事实说明”和“操作流程说明”分离。

## 验证原则
- 优先做无副作用验证，如静态阅读、局部命令帮助检查、语法检查。
- 未经用户明确同意，不执行可能改写价格库、覆盖同步产物或触发外部同步的命令。
